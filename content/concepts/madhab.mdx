# Madhab

Madhab controls the Asr calculation setting. v1 supports `shafi` by default and
`hanafi` when required by the caller's practice.

```text
madhab=shafi   # default
madhab=hanafi
```

The setting primarily changes Asr's shadow-length calculation. It does not select a
calculation method and does not alter the meaning of Azan adjustments.
