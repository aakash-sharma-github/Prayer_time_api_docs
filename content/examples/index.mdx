# Examples

These examples use the production API at `https://api.aakashsharma.com.np`. They
demonstrate request construction and response handling; refer to
[API Reference](/api-reference) for the complete contract. For local development,
replace the production origin with `http://127.0.0.1:8000`.
