# First API Request

Use the versioned `GET /api/v1/prayer-times` endpoint for a single local date.

```bash
curl "http://127.0.0.1:8000/api/v1/prayer-times?latitude=25.2048&longitude=55.2708&timezone=Asia/Dubai&calculation_method=dubai"
```

The exact request and response contract will be supplied from the API OpenAPI schema.
